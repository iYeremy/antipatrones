public class UserAddressValidator {
    // Clase demasiado vaga, solo hace una cosa que ya hace el lenguaje
    public boolean isValid(String address) {
        return address != null && !address.isEmpty();
    }
}
